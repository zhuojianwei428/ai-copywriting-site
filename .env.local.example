# 复制本文件为 .env.local 并填入你的真实值
DEEPSEEK_API_KEY=sk-XXXXXXXXXXXXXXXX

# Clerk Auth（登录功能，见下方配置说明）
# 到 https://dashboard.clerk.com 建应用后，在 Configure → API Keys 复制两个值：
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXX
CLERK_SECRET_KEY=sk_test_XXXXXXXXXXXXXXXX
# 注意：publishable key 设计上就是给浏览器用的（必须带 NEXT_PUBLIC_ 前缀）；
# CLERK_SECRET_KEY 是服务端密钥，绝不能加 NEXT_PUBLIC_。
#
# 两个 key 必须同时配置，登录墙才会生效：
#   - 只配 publishable key：前端会弹登录框，但服务端无法校验会话（按未配置处理，不拦 API）。
#   - 两个都缺：全站游客模式，登录墙完全不生效（可用作回滚开关）。
#
# 还需要在 Clerk 后台做的两件事（不写在这两个变量里）：
#   1. User & Authentication → Social connections → 打开 Google（Clerk 托管 OAuth，
#      不需要自己去 Google Cloud 建客户端）。
#   2. 生产域名要在 Clerk 里完成验证，它会给一组 DNS 记录让你加到域名解析
#      （SPF / DKIM 等，用于保证验证码邮件送达率）。

# 可选覆盖项：
# DEEPSEEK_MODEL=deepseek-v4-flash
#   注意：deepseek-chat / deepseek-reasoner 已于 2026-07-24 停用，调用会直接报错。
#   现役模型：deepseek-v4-flash（默认，便宜）/ deepseek-v4-pro（约 3 倍价格）。
# DEEPSEEK_BASE_URL=https://api.deepseek.com
#
# 成本防护（均可选项，括号中为默认值）：
# MAX_INPUT_CHARS=6000            # 单次请求输入字符上限，超出返回 413
# MAX_OUTPUT_TOKENS=1600          # 单次生成最大输出 token（7 部分完整绩效表需 ~1300）
# DAILY_CALL_WARN_THRESHOLD=500   # 单日调用次数告警阈值，达到即在日志输出 WARN
#
# 安全约束：DEEPSEEK_* 与 CLERK_SECRET_KEY 一律不得以 NEXT_PUBLIC_ 开头，
# 只能作为服务端环境变量存在；唯一允许公开的是 NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
#（设计上就是给浏览器的）。
